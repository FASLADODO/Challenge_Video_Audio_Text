Code pour évaluation des clusters

Créer un fichier pour chaque cluster : 
ecrire dans un fichier pour voir se que j'observe sur mes clusters

wordnet
francais -> trad en anglais (avec les sous-titres)



scatter = dict(
    mode = "markers",
    name = "y",
    type = "scatter3d",
    x = df['Svd_0'], y = df['Svd_1'], z = df['Svd_2'],
    marker = dict( size=2, color="rgb(23, 190, 207)" )
)
clusters = dict(
    alphahull = 7,
    name = "y",
    opacity = 0.1,
    type = "mesh3d",
)
layout = dict(
    title = '3d point clustering',
    scene = dict(
        xaxis = dict( zeroline=False ),
        yaxis = dict( zeroline=False ),
        zaxis = dict( zeroline=False ),
    )
)

fig = dict( data=[scatter, clusters], layout=layout )
# Use py.iplot() for IPython notebook
py.plot(fig, filename='3d point clustering')
